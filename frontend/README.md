# Frontend (TypeScript)

Browser client for the 3D cube-surface multiplayer snake game.

## Prereqs

- Node.js 18+ recommended

## Install

```bash
cd frontend
npm install
```

## Run (dev)

```bash
cd frontend
npm run dev
```

Then open the printed URL and connect to the backend at `http://localhost:8000`.

